spring:
  config:
    activate:
      on-profile: dev

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  datasource:
    url: ${db.local.url}
    driver-class-name: org.mariadb.jdbc.Driver
    username: ${db.local.username}
    password: ${db.local.password}
