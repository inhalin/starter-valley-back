spring:
  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format-sql: true

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/valley
    username: root
    password:

app:
  oauth2:
    client:
      github:
        client-id: 53f31b9dbff19b8be1e0
        client-secret: